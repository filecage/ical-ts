import TimezoneDefinitionBase from "./TimezoneDefinitionBase";
import {ComponentName} from "./ComponentName";

export default class TimezoneDaylight extends TimezoneDefinitionBase {
    public readonly key = ComponentName.TZONE_DAYLIGHT;
}